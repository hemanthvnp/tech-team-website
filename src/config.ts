// src/config.ts
// Site-wide feature flags. Flip a value and restart/rebuild.

/** The opening Star Wars crawl. Off: the hero loads straight in its landed state. */
export const ENABLE_INTRO_ANIMATION = false;

/** The site navbar. Off: it is not rendered on any page. */
export const ENABLE_NAVBAR = false;
